%div
  %h1
    = @homework.title
  = @homework.question

  - if @assignments.blank?
    No submissions yet.
  - else
    - if current_user.role == 'student'
      = render partial: "student_submissions", locals: {assignments: @assignments}
    - else
      = render partial: "teacher_submissions", locals: {assignments: @assignments }
